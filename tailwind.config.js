/** @type {import('tailwindcss').Config} */
tailwind.config = {
   theme: {
      extend: {
         fontFamily: {
            poppins: 'Poppins, sans-serif',
            sans: 'Graphik, sans-serif'
         }
      }
   }
}