/** @type {import('tailwindcss').Config} */
tailwind.config = {
   theme: {
         fontFamily: {
            poppins: 'Poppins, sans-serif',
            sans: 'Graphik, sans-serif'
         },
   }
}